#!/usr/bin/env node

import * as streamconsumers from "node:stream/consumers";
import fs from "node:fs/promises";
import process from "node:process";
import util from "node:util";

import Anthropic from "./library/Anthropic.js";
import Gemini from "./library/Gemini.js";
import OpenAI from "./library/OpenAI.js";

import * as Execute from "./library/Execute.js";
import * as Patch from "./library/Patch.js";

let providers = [
  {
    runner: OpenAI,
    prefix: "gpt-",
    apiKey: process.env.OPENAI_API_KEY
  },
  {
    runner: Gemini,
    prefix: "gemini-",
    apiKey: process.env.GEMINI_API_KEY
  },
  {
    runner: Anthropic,
    prefix: "claude-",
    apiKey: process.env.ANTHROPIC_API_KEY
  }
];

let tools = { execute: Execute, patch: Patch };

async function callTool(name, args) {
  try {
    let tool = tools[name];
    if (tool === undefined)
      throw new Error(`Tool not found: ${name}`);

    if (!tool.validateArgs(args)) {
      console.log(`${name.toUpperCase()} [invalid arguments]`);
      throw new TypeError(`Invalid arguments: expected ${JSON.stringify(tool.schema)}`);
    }

    console.log(`${name.toUpperCase()} ${tool.argsToString(args)}`);

    return await tool.call(args);
  } catch (error) {
    if (typeof error.toJSON === "function")
      return { error };
    else
      return { error: String(error) };
  }
}

function message(message, options = {}) {
  if (options.type !== undefined) {
    console.log(`${options.type.toUpperCase()} ${message}`);
  } else {
    console.log();
    console.log(message);
    console.log();
  }
}

function modelRunner(model) {
  let provider = providers.find(p => model.startsWith(p.prefix));
  if (provider === undefined)
    throw new Error(`Unknown model name: ${model}`);

  return provider.runner({
      apiKey: provider.apiKey,
      model,
      tools: Object.values(tools).map(t => t.schema),
      callTool,
      message
  });
}

function readFiles(paths) {
  return Promise.all(paths.map(path => fs.readFile(path, "utf8")));
}

async function main() {
  let { values: options, positionals } = util.parseArgs({
    options: {
      verbose: { type: "boolean" },
      model: { type: "string", default: "gpt-5.2-codex" },
      system: { type: "string", multiple: true },
      file: { type: "string", multiple: true },
      "system-file": { type: "string", multiple: true }
    },
    allowPositionals: true
  });

  let run = modelRunner(options.model);
  let prompt = {
    system: [],
    user: positionals
  };

  if (options.system !== undefined)
    prompt.system.push(options.system);

  if (options["system-file"] !== undefined)
    prompt.system.push(...await readFiles(options["system-file"]));

  if (options.file !== undefined)
    prompt.user.push(...await readFiles(options.file));

  if (!process.stdin.isTTY)
    prompt.user.push(await streamconsumers.text(process.stdin));

  try {
    let { tokens } = await run(prompt, tools, message);
    console.log(`DONE ${tokens} tokens`);
  } catch (error) {
    console.error(options.verbose ? error.stack : error);
  }
}

await main();
